const fs = require('fs'); const cheerio = require('cheerio'); const content = fs.readFileSync('c:/workspace/itstory/computers.html', 'utf-8'); const $ = cheerio.load(content); let totalLength = 0; .each((i, el) => { totalLength += .text().length; }); console.log('Total text length:', totalLength);